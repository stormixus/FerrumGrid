//! Info panel (selected cell / row metadata + edit affordances) (Plan v7
//! Phase 1.95c — TODO).
//!
//! Will host: `render_info_panel` and its sub-renderers (table overview,
//! columns, indexes, relations, rules/triggers, JSON tree, enum control).
//! Currently all functions remain in `super::mod.rs`. Target ≤500 lines.
